Que 3. Create a webpage to find the Factorial of a number.

    * Html part :
        1 - Create an input box for the user input (Give ID to the input box).
        2 - Create a button for triggering the calculation part (give ID to the button).
        3 - Show the output in h1 (give an ID to h1)

    * JS part :
        1 - target the button using ID
        2 - Add event listener to the button
        3 - Create a variable to store the input from the input box in html using the ID and convert it into number format from string format
        4 - Create a function to perform the calculation
        5 - And store the answer in another variable.
        6 - Show the answer in h1 by using it's ID 
